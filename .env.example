# 复制本文件为 .env 并填入真实值即可接入真实大模型
# 未配置 AI_API_KEY 时，系统自动降级为「本地模拟模式」，无需密钥也能完整跑通全链路

# OpenAI 兼容接口的密钥（任意兼容 /chat/completions 的服务均可）
AI_API_KEY=

# 接口地址（不含 /chat/completions 后缀）
AI_BASE_URL=https://api.openai.com/v1

# 模型名
AI_MODEL=gpt-4o-mini

# 服务端口
PORT=8000
